import React from 'react';

import Shift from './shift/Shift';

const shifts = (props) => {
	return (
		<React.Fragment>
			<p>Shifts Component</p>
			<Shift />
		</React.Fragment>
	);
}

export default shifts;